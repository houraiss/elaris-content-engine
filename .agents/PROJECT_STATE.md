# Elaris Content Engine — Global Project State

This file serves as the definitive source of truth for the project's current state, architecture, and history. **All AI models must read this file at the start of a conversation** to carry on from where the last one left off.

## 1. Project Overview
- **Name**: Elaris Content Engine v3.0 (@elaris.925)
- **Purpose**: An Instagram content studio designed for Elaris Jewelry Store. It transforms raw photos into branded, high-quality social media posts.
- **Tech Stack**: Vanilla HTML, CSS, JavaScript. No complex build steps or frameworks. It features a custom lightweight JS router (`app.js`) and PWA support via `sw.js` and `manifest.json`.

## 2. Core Modules
The application is structured into several core views:
- **Prompt Studio (`js/prompt-studio.js`)**: For generating AI prompts. Includes logic for "Archetypes", "Smart Guides", "Stones", and "Materials".
- **Motion Studio (`js/motion-studio.js`)**: Module for video/motion graphics generation.
- **Captions (`js/captions.js`)**: Generates branded captions and hashtag sets based on product type and brand voice (Luxury, Conversational, Storytelling).
- **Trends (`js/trends.js` or via `assets/trends.json`)**: Tracks current jewelry & design trends. Warns users if data is older than 30 days.
- **Batch Mode (`js/batch.js`)**: Handles bulk operations.
- **Watermark (`js/watermark.js`)**: For applying branded watermarks to images.
- **Generate (`js/generate.js`)**: Logic for content generation pipelines.
- **Export (`js/export.js`)**: Utilities for copying to clipboard, downloading files, etc.
- **Internationalization (`js/i18n.js`)**: Supports multiple languages (EN, FR, AR).

## 3. Important Architectural Rules & Patterns
- **Archetype Integration**: When adding a new archetype, it requires a version badge (e.g., "V3.0"), a calibrated Smart Guide (camera angle, framing), normalized stone/material labels (human-readable), and connection to dynamic camera/lighting.
- **UI/UX**: Uses a dark/light mode toggle. Uses PWA features for mobile compatibility.
- **Git Workflow**: The AI is instructed to automatically run `git add`, `git commit`, and `git push` at the end of every task to ensure no updates are lost between sessions.

## 4. Update Log
*Add an entry here every time you make a significant change, implement a new feature, or refactor architecture. Always include the date/time and a concise summary.*

- **2026-07-18**: Created `PROJECT_STATE.md` to serve as the global context file for all future AI agents. Updated `.agents/AGENTS.md` to mandate reading this file at startup.
- **2026-07-18**: Added 5 new Watch archetypes with a dedicated `WATCH` badge. Expanded `prompt-studio.js` with new watch-specific camera angles and lighting moods. Updated `guideDB` and `archetypeAngleBoost` to fully integrate watch logic, and added `.ps-watch-badge` styling to `styles.css`.
- **2026-07-18**: Fixed watch archetypes pipeline to ensure dynamic parameters (Brand identity, Model details) are injected into the prompt, and linked lighting options to the dynamic recommendations.
- **2026-07-18**: Fixed Watch category fallbacks so that placement rules, negative prompts, and hallmark instructions correctly refer to watches instead of silver rings. Updated score/ranking logic so Watch archetypes properly rank at the top when the Watch category is selected. Also linked watch archetypes to the UI lighting chips recommender.
- **2026-08-22**: Added **Jewelry Set** as a new 10th category (`'jewelry-set'`). Full implementation includes:
  - 4 new SET archetypes: `set-editorial-display`, `set-worn-collection`, `set-gift-presentation`, `set-detail-showcase`, each with 6 unique subjects, a rich scene, and fully calibrated compat scores.
  - Amber/rose-gold `.ps-set-badge` ("SET") displayed on archetype cards; `ps-arch-set` class added.
  - Sub-piece chip selector (`_renderSetComposition`, `_toggleSetPiece`) that appears when Jewelry Set is selected, letting the user choose Ring/Necklace/Earrings/Bracelet/Bangle.
  - Dynamic `_autoDescribe` that reads `state.setComposition` to list all pieces in the generated description.
  - `_buildPlacementInstruction` and `_buildCategoryNegatives` entries for `jewelry-set`.
  - Smart Guide (`guideDB`) entries for all 4 new archetypes.
  - `_getAnglesForCategory` and `_getLightingForCategory` rankings for `jewelry-set`.
  - `archetypeAngleBoost` and `archetypeLightingBoost` entries for 4 new archetypes.
  - `hallmarkMap` entry (engraved on ring band, necklace clasp, earring posts).
  - `set-worn-collection` added to `humanArchetypes` (uses model anatomy safety rules).
  - Category label normalized to "Jewelry Set" in the dropdown.
  - `catLabels` updated with `'jewelry-set': 'matching jewelry set'`.
  - Service worker cache bumped to `elaris-v34`.
  - All 70 existing archetype `compat` objects updated with `'jewelry-set': N` scores.
- **2026-08-23**: Fixed "Coming Soon" error on Prompt Studio load caused by a missing comma in the `HUMAN` set in `prompt-studio.js`. Cleaned corrupted object structures in `archetypeAngleBoost` and correctly wired `guideDB` Smart Guide entries for all SET archetypes. Added `set` and `jewelry-set` support to Caption Studio (`captions.js` descriptions and hashtags, `app.js` product type dropdown), added i18n keys for EN, FR, and AR, added router alias handling so `#set` and `#jewelry-set` route directly to Prompt Studio with category pre-selected, cache-busted script versions in `index.html`, and bumped service worker cache to `elaris-v35`.
- **2026-08-24 (Update 2)**: Refined **Prompt Studio Beta** according to user feedback:
  - Removed top "⚡ 1-Tap Presets" bar and cleaned up interface.
  - Built an interactive **70+ Archetype Liquid Glass Modal** with real-time search filtering, category tabs (Model, Product, Nature, Mood, Sets, Watches), and click-to-select with automatic carousel synchronization.
  - Restored all missing Deep Modifiers organized into 4 categorized tabs:
    - **Model & Hijabi**: Female/Male/No Model, 6 Skin Complexions, 7 Facial Expressions, 🧕 Hijabi toggle + 5 Wrap Styles.
    - **Camera & Light**: 35+ Lighting Moods, 9 Lens Profiles (85mm, 100mm Macro, 135mm, 50mm, 35mm, Anamorphic 40mm, Phase One IQ4), 30+ Shot Angles.
    - **Styling & Scene**: 14 Wardrobe Presets, 9 Curated Color Palettes, 13 Backdrop Surfaces, Jewelry Style multi-select chips.
    - **Brand & Details**: 925 Hallmark toggle, Brand Identity Touch with 4 placement options, and Micro-Realism elements (Pores, Veins/Freckles, Arm Hair, Wrinkles).
  - Enhanced **Smart Guide Live**: Dynamically displays calibrated Angle, Lighting, Camera, Compatibility score, and 3 actionable Director Photography Tips.
  - Integrated 7 Aspect Ratios: `1:1`, `4:5`, `9:16`, `16:9`, `2:3`, `3:4`, `21:9`.
  - Overhauled mobile responsiveness: 1-column responsive layout, touch-friendly scrolling, and floating 5-button iOS 26 capsule bottom dock.
  - Added `promptstudiobeta` to router valid routes in `js/app.js` enabling direct URL navigation.
- **2026-08-28**: **Studio Beta v2 — Full Responsive, Feature Parity & Design Overhaul**:
  - **`css/beta.css` → v2**: Complete responsive rewrite — 3-col grid collapses cleanly to 1-col on mobile (≤768px); aspect ratio bar wraps on narrow screens; modifier tabs scroll horizontally; mobile dock has iOS safe-area padding. Full light/dark mode contrast overhaul: chips, section titles, modal cards, toggle descriptions, score badges, and active states all readable in both themes. Added CSS vars `--psb-chip-bg/border/color`. History copy button always visible (opacity 0.55 base).
  - **`js/prompt-studio-beta.js` → v2**: Fixed critical expert drawer bug (flex-column caused all tabs to stack — now uses CSS class `.active` on `.psb-mod-content`). Added Jewelry Style multi-select chips (12 options). Added Variation Count 1–5 with partial UI update. Added Prompt Quality Level chips (Standard/Detailed/Ultra). Debounced modal search for mobile keyboard focus. Added `_renderModalGrid()` partial update. Enhanced prompt builder includes jewelry styles, wardrobe, quality prefix, and micro-realism. All 5 mobile dock buttons wired correctly.
  - **`index.html`**: `beta.css?v=2`, `prompt-studio-beta.js?v=2`.
- **2026-08-31**: **Studio Beta v4 — Bugfix & Polish Patch**:
  - **`js/prompt-studio-beta.js` → v4**: Removed critical corrupted duplicate code block at line 1260 (paste artifact from previous session that produced a malformed `_renderModal` tail). Fixed Expert dock mobile button to scroll the toggle element with `block:'nearest'` instead of the drawer itself, preventing full-page scroll jump.
  - **`css/beta.css` → v4**: Added missing styles for `.psb-modal-sort-bar`, `.psb-modal-sort-btn` (with active state using purple accent), and `.psb-modal-count` — making the V3 filter chip, A-Z sort button, and archetype count label in the Full Library modal fully visible and styled.
  - **`index.html`**: Bumped `beta.css?v=4`, `prompt-studio-beta.js?v=4`.
- **2026-08-31**: **Studio Beta v5 — Major Functionality Fixes**:
  - **`js/prompt-studio-beta.js` → v5**: Added `_inferCategory()` to derive archetype categories from ID (archetypes in prompt-studio.js lack a `.category` field) using `_HUMAN_IDS`, `_ORGANIC_IDS`, `_MOOD_IDS` Sets and watch-/set- prefixes. Fixed `_filterArchetypes()` to use inferred categories — all 6 modal filter tabs now work. Closed missing `</div>` for Camera tab content that caused Styling & Brand tabs to be blank. Removed Design Details textarea (unused by user). Added dynamic score badge coloring: green `#34d399` for ≥85%, gold `#fbbf24` for 75–84%, silver `#94a3b8` for <75%. Added V3/WATCH/SET type badges on carousel and modal cards. Removed dead `desc` input binding.
  - **`css/beta.css` → v5**: Added `.psb-arch-type-badge`, `.psb-badge-v3`, `.psb-badge-watch`, `.psb-badge-set` styles with light/dark mode support. Badges float at top-right of carousel cards.
  - **`index.html`**: Bumped `beta.css?v=5`, `prompt-studio-beta.js?v=5`.
- **2026-08-31**: **Studio Beta v6 — Scoring Rewrite & Smart Guide Fix**:
  - **`js/prompt-studio-beta.js` → v6**: Rewrote `_calculateArchetypeScore()` to read `arch.compat[category]` directly (real data: 25–95 range) instead of keyword matching starting at 75. Clamp changed to 25–99 for real visual spread. Fixed `_getGuideData()` to: (1) call `window.PromptStudio._getGuideDB()` as primary source (returns full data once `renderSmartGuide` runs); (2) fixed category-based fallback to use `_inferCategory()` instead of `arch.category` (always empty) — all 64+ uncovered archetypes now get correct category-specific tips.
  - **`js/prompt-studio.js` → v63**: Added `this.guideDB = guideDB` inside `renderSmartGuide()` to cache the full guide data (angle+lighting+camera+tips for all 70+ archetypes). Updated `_getGuideDB()` to return `this.guideDB` when available (full data), falling back to partial lighting-only data.
  - **`index.html`**: Bumped `prompt-studio.js?v=63`, `prompt-studio-beta.js?v=6`.
- **2026-09-03**: **Studio Beta v7 — Full Modifier Import & Smart Guide Apply Button**:
  - **`js/prompt-studio-beta.js` → v7**: Added 10 modifier getters pulling all modifiers from master PromptStudio (53 angles in 8 groups, 38 lighting moods in 5 categories, 9 camera profiles, 13 surfaces, 10 palettes, 10 wardrobe styles, 6 hijab styles, 3 brand touches, 6 ethnicities, 7 facial expressions). Rewrote all 4 modifier tab renders dynamically. Added `_getLabelForAngle/Lighting/Camera()` resolvers. Wired `#psb-apply-guide-btn` click to auto-apply recommended angle, lighting, and camera to dropdowns and open expert drawer.
  - **`js/prompt-studio.js` → v64**: Pre-initialized `PromptStudio.guideDB` on script load for cross-studio data access.
  - **`css/beta.css` → v6**: Added `.psb-guide-apply-btn` styling with gold border.
  - **`index.html`**: Bumped `prompt-studio.js?v=64`, `prompt-studio-beta.js?v=7`, `beta.css?v=6`.
- **2026-09-03**: **Studio Beta v8 — CSS Overhaul & Brand Tab JS Fix**:
  - **`js/prompt-studio-beta.js` → v8**: Fixed critical corruption in Brand tab template literal — removed `sb-slider">` text leak (corrupt paste artifact) and removed the entire duplicate brand-touch conditional block. Brand tab now cleanly renders: Hallmark toggle → Brand Identity toggle → conditional Brand Touch select → Micro-Realism chips.
  - **`css/beta.css` → v7**: Comprehensive CSS fixes: (1) 3-column grid explicit `grid-column` assignments for `psb-left/center/right-col` for reliable layout; (2) breakpoints refined — right col hides at `940px` not `1024px` so it shows at more desktop widths; (3) Smart Guide stats use CSS grid `auto 1fr` layout so labels and values align neatly; (4) `⚡ Apply Recommended Setup` button significantly more prominent (stronger gold border, box-shadow, bolder font); (5) `.psb-expert-toggle.active` alias added alongside `.open`.
  - **`sw.js`**: Bumped cache to `elaris-v36` to force all browsers to fetch fresh JS/CSS.
  - **`index.html`**: Bumped `beta.css?v=7`, `prompt-studio-beta.js?v=8`.

- **2026-09-04**: **Studio Beta v9 — Bug Fixes + Smart Guide Expansion + Camera Expansion**:
  - **Bug Fix — Aspect Ratio not reaching prompt**: Added `_AR_TO_FORMAT` map object. When aspect ratio chips clicked, now syncs both `state.aspectRatio` AND `state.format`. Also fixed `_buildSinglePrompt` to always convert via `_AR_TO_FORMAT[aspectRatio]` before passing to master `_buildPrompt()` (which reads `state.format`, not `state.aspectRatio`).
  - **Bug Fix — Lighting Mood filter broken**: Removed master `window.PromptStudio.lightingMoods` fallback from `_getLightingMoods()`. Master data lacks `.category` field required by filter chips. Beta always uses local categorized list.
  - **Camera Profiles expanded**: Added 6 new lenses: Nikon Z9 85mm f/1.8S, Fujifilm GFX 100S 110mm, Zeiss Otus 55mm APO, Tilt-Shift 90mm TS-E, Sigma 85mm f/1.4 Art, Voigtländer 75mm f/1.5. Camera dropdown now removes master data fallback too (consistent labeling).
  - **Smart Guide Live expanded**: Added `_V3_IDS` set mirroring master studio. Smart Guide now shows: V3.0 badge on archetypes, "Best For" field, "Scene Intelligence" section (Compatibility, Category, Piece Synergy), "Optimal Setup" section (Angle #1 & #2, Lighting #1 & #2, ⭐ Lens, Alt Lens, Depth of Field, ISO Range). Added `_getGuideDOF()` and `_getGuideISO()` helpers.
  - **Camera Lens dropdown**: ⭐ marks recommended lens, ✦ marks alt lens for the active archetype's guide data.
  - **Archetype Modal cards**: Added `bestFor` (yellow tag) and `desc` (description excerpt) to both modal card render locations.
  - **CSS additions** (`beta.css`): `.psb-guide-section-label`, `.psb-guide-bestfor`, `.psb-guide-v3-badge`, `.psb-guide-apply-btn` (full-width block).
  - **`sw.js`**: Bumped cache to `elaris-v37`.
- **2026-09-07**: **Studio Beta v10 — DOF/ISO controls + AI-Chooses defaults + 5 new Deep Modifiers**:
  - **`js/prompt-studio-beta.js` → v10**: Camera tab gained selectable Depth of Field and ISO Range dropdowns (previously display-only). "AI Chooses (Max Diversity)" is now the default for Wardrobe & Outfit, Color Palette Harmony and Surface & Backdrop. Added 5 new modifiers with full fallback-builder integration: Body Part Focus (Model tab), and Film Style & Grade, Environment / Background, Mood Intensity, Season & Time of Day (Styling tab). New getters `_getBodyFocusOptions/_getDOFOptions/_getISOOptions/_getFilmStyleOptions/_getEnvironmentOptions/_getMoodIntensityOptions/_getSeasonTimeOptions`.
- **2026-09-07**: **Studio Beta v10.1 — Model Consistency**:
  - **`js/prompt-studio-beta.js` → v10.1** + **`css/beta.css`**: Added a "🧠 Model Consistency" toggle + profile-card selector in the Smart Guide area. 10 built-in Moroccan model profiles (5 female: Lina, Sara, Nour, Malak, Rania; 5 male: Amir, Tariq, Younes, Mehdi, Karim), each with a detailed physical descriptor. Profiles are shared with the master studio via `localStorage` key `elaris_model_profiles` (built-ins merged with saved overrides + customs). When enabled, the active profile's descriptor is injected into the prompt as a "sole appearance reference — match exactly" line, and synced to `PromptStudio.state` (`consistencyOn`, `activeProfileId`, `profiles`).
- **2026-09-07**: **Studio Beta v10.2 — Deep Modifier reachability fix**:
  - **`css/beta.css`**: `.psb-expert-drawer` `overflow: hidden` → `visible`; `.psb-mod-content` tab panels made scrollable (`max-height: 520px; overflow-y: auto`) with a thin gold scrollbar, so options at the bottom of a modifier tab are always reachable.
- **2026-09-10**: **Studio Beta v11 — Live Trends integration + Trends page refresh**:
  - **`assets/trends.json`**: Refreshed to 2026-09-10 with 10 current fine-jewelry / Instagram content trends researched from the web (whowhatwear, marieclaire, forbes, timothy-hogan, squareshot, gemfind, hootsuite, pixofix — recorded in a new `sources` array). **New schema**: each trend keeps its existing fields and gains a `studio` block — `directive` (sentence injected into the prompt) plus optional `lightingMood / surface / palette / angle / cameraProfile / moodIntensity / seasonTime / filmStyle / environment / styling` overrides and a `jewelryStyle` array, all using valid Studio Beta option IDs.
  - **`js/prompt-studio-beta.js` → v11**: Added `_loadTrends()` (fetches `assets/trends.json` on init), `_applyTrend()` / `_clearTrend()` / `_getActiveTrend()` / `_getTrendChanges()`. New **"🔥 Live Trends" panel** rendered by `_renderTrendsPanel()` in the right column under the Smart Guide: trend cards (icon + title + relevance dot), and when one is active a directive quote, "what it changed" chips and a Clear button. Selecting a trend auto-applies its `studio` recipe to live modifier state (merges `jewelryStyle`) **and** injects its `directive` into every generated prompt — on the master path via `ps.pieceDesc` (blended into the positive body, master `pieceDesc` restored after build so the master studio isn't polluted); on the fallback builder via a `parts.push` before the `--ar` flags. A "🔥 Trend active: …" indicator also shows above the Generate button (center column, so it's visible on mobile where the right column is hidden). Added the missing `_showToast()` helper (was called by the Smart Guide "Apply Recommended Setup" button but never defined — that click threw a console error).
  - **`js/app.js`**: Each Trends-page card with a `studio` block gets a "🔥 Use in Studio Beta →" button that stores the trend id in `localStorage` (`elaris_active_trend`) and routes to `#promptstudiobeta`; Studio Beta's `_loadTrends()` consumes that hand-off on entry (applies the trend, clears the key). Stale-data banner copy updated (no longer references "Antigravity"; notes the `studio` block must be preserved on refresh).
  - **`index.html`**: `beta.css?v=8`, `prompt-studio-beta.js?v=11` (both were still on the v8-era query strings). **`sw.js`**: cache `elaris-v37` → `elaris-v38`.
  - **`.gitignore`**: ignore `.claude/` (local preview-launch helper).
- **2026-09-10**: **Trends v12 — per-trend reference links ("see it first")**:
  - **`assets/trends.json`**: each trend gained a `reference` object `{ label, url }` pointing to the specific web article the trend was drawn from (Who What Wear, Forbes, GemFind, Photta, Squareshot ×2, Pixofix, Hootsuite, Honho, Marie Claire — all link-checked). `sources` array updated to match.
  - **`js/app.js`**: Trends-page cards render a "🔗 See this trend in the wild" link (opens the reference in a new tab, `rel="noopener noreferrer"`) above the "Use in Studio Beta" button.
  - **`js/prompt-studio-beta.js` → v12**: the Live Trends panel's active-trend box shows a "🔗 See it first — <source> ↗" link so the reference is reachable from inside the Studio before generating.
  - **`css/styles.css`**: `.trend-ref-link` / `.trend-ref-src`. **`css/beta.css`**: `.psb-trend-ref`.
  - **`index.html`**: `styles.css?v=20`, `beta.css?v=9`, `app.js?v=5`, `prompt-studio-beta.js?v=12`. **`sw.js`**: cache `elaris-v39` (app.js is in the SW app-shell, so the bump is required to ship the app.js change).
- **2026-09-10**: **Trends v13 — Social & Viral trends category**:
  - **`assets/trends.json`**: new top-level `groups` map (`jewelry`, `social`) and a `group` field on every trend. Added 6 **Social & Viral** trends — flexible cross-industry formats, not jewelry-specific: `80s-ai-portrait` (the viral ChatGPT/AI 80s photo trend), `retro-era-swap` (any-decade time-travel carousel), `ai-melt-metal` (AI melt/squish effect as a molten-silver reveal), `kinda-chic` (romanticise-the-ordinary content format), `gold-star-behaviour` (community/values format, info-only), `ghibli-painterly` (painterly AI filter, info-only, low relevance). Four carry `studio` blocks so they work in the Studio; two are content-format only (no `studio`, no "Use in Studio Beta" button). Each has a link-checked `reference` (India TV, LatestLY, Vidguru, Pepper Agency). `contentCalendarTip` updated ("ride one Social & Viral trend per week max — keep the feed mostly photographic").
  - **`js/app.js` v6**: Trends page now renders **grouped sections** with headers + blurb (`💎 Jewelry & Design Trends`, then `📱 Social & Viral Trends`), driven by `data.groups` + each trend's `group` (with an orphan catch-all). Card renderer refactored into `renderCard()`. New category icons (`viral-format`, `ai-filter`, `audio`). Info-only trends show "✎ Content format — no render needed" instead of the Studio button.
  - **`js/prompt-studio-beta.js` v13**: Live Trends panel gets **group-filter chips** (All / 💎 Jewelry / 📱 Social), shown only when >1 group is present; `state.trendGroupFilter`. Social trends with a `studio` block apply exactly like jewelry ones (info-only trends are already filtered out of the panel by the existing `studio.directive` guard).
  - **`css/styles.css`**: `.trend-section-title` / `.trend-section-sub`. **`css/beta.css`**: `.psb-trend-groupbar` / `.psb-tg-chip`.
  - **`index.html`**: `styles.css?v=21`, `beta.css?v=10`, `app.js?v=6`, `prompt-studio-beta.js?v=13`. **`sw.js`**: cache `elaris-v40`.
- **2026-09-10**: **i18n rebuild — Phase 1 of N (engine + shell + Trends + RTL)**. The multi-language feature was ~10% wired: dictionaries existed (EN/FR/AR, ~340 keys) but nothing re-rendered on switch, and most modules had zero `data-i18n`. Decision: rebuild properly, module by module, translating **UI chrome** to FR + AR (live editorial content like trend copy stays source-language).
  - **`js/i18n.js` (engine v2)**: replaced the `I18nManager` class with an object exposing `t(key, en)` — **English lives at the call site**, so only `fr`/`ar` need dict entries (missing key → English arg → `en` dict → key). `setLanguage()` now persists, flips `<html dir/lang/data-lang>`, updates static `[data-i18n]` nodes (now also supports `data-i18n-attr` for placeholder/title/aria-label + `data-i18n-en`), **then re-renders the active page** via `Elaris.loadPage(currentPage)` so JS-built views rebuild in the new language. New strings go in labelled `Object.assign(translations.fr/ar, {…})` blocks at the end of the file, never by editing the 1200-line literal. `window.t` global alias added. `RTL_LANGS = ['ar']`.
  - **`js/app.js` v7**: `loadPage()` calls `I18n.applyLanguage()` after every render and translates its own loading / error / "coming soon" strings. `render_trends()` fully wired with `t()` — category names, relevance line (`{level}` placeholder for FR/AR word order), section headers + blurbs (`trd_group_<key>_label/_blurb`), suggestion label, "see this trend", "use in Studio Beta", "no render needed", staleness banner, footer, empty state.
  - **`css/rtl.css`** (new, linked after beta.css): RTL pass for Studio Beta (beta.css had zero RTL), the Trends page and shared components, plus global direction/bidi hygiene (Latin/code/number runs kept LTR, sliders fill from the right, compiled-prompt output stays LTR). The flex sidebar and the `psb-layout` grid reverse for free; `styles.css` already carried a small first RTL block which this extends.
  - **`index.html`**: `data-i18n` added to the Studio Beta / Motion Studio nav labels and the loading text; `rtl.css?v=1` linked; `i18n.js?v=7`, `app.js?v=7`. **`sw.js`**: `css/rtl.css` added to the app-shell precache; cache `elaris-v41`.
  - **Verified**: live EN→FR→AR switch re-renders in place (no reload); Trends page fully trilingual; the *old* Prompt Studio's pre-existing i18n now actually works live; Arabic flips the whole layout RTL (sidebar → right, 3-col studio grid reverses) with no breakage; no console errors.
  - **Remaining phases** (English-only until done, no regression): P2 Studio Beta strings, P3 old Prompt Studio gaps, P4 Motion Studio / Generate / Enhance / Batch / Watermark / PWA, P5 Arabic native-review pass. Pattern for each: wrap render-template strings in `t('key','English')`, add `fr`/`ar` to an `Object.assign` block, extend `rtl.css` for any new directional components.
- **2026-09-10**: **i18n Phase 2 — Studio Beta UI wired to FR + AR** (`prompt-studio-beta.js` v14).
  - Added `_t(key, en)` helper on the module. Wrapped ~150 UI-structure strings across `_render()`, `_renderTrendsPanel()`, `_renderModal()`, `_getTrendChanges()`, `_updateCountUI()`, `_updateModalCount()`, `_renderModalGrid()` and all toasts: header/subtitle, left-panel labels (Category / Precious Metal / Center Gemstone / Aspect Ratio / Detail Level / Realism Engine + chip labels), carousel header + "% Match", the 4 modifier tab names, every field **label** and hint in the Model / Camera / Styling / Brand tabs, toggle titles + descriptions (Hijabi, Model Consistency, 925 Hallmark, Brand Identity), lighting filter chips + optgroup headers, angle-group headers, Smart Guide (section labels, all stat labels, synergy words, Apply button + title, Director Tips heading + the 3 fallback tips, Recent Generations, empty state), the archetype-library modal (title, search placeholder, category tabs, V3/A–Z/count, empty state), the mobile dock, the trend panel's own strings (reusing the Phase-1 `trd_*` keys), and generate/copy/applied toasts.
  - **Scope line held deliberately**: the **modifier option *values*** (53 shot angles, 38 lighting moods, 15 lens profiles, surfaces, palettes, wardrobe, environments, film stocks, etc. — mostly photography proper-nouns) and per-archetype names / taglines / guide tips (which live in `prompt-studio.js` data) stay English for now. Everything a user needs to *navigate and operate* the studio is translated.
  - **`js/i18n.js` v?=8**: new `Object.assign(translations.fr/ar, {…})` block "Phase 2 · Studio Beta" (~155 keys each). Fixed a Phase-1 double-emoji bug in `trd_active_prefix` (dropped the leading 🔥, since the template already renders it).
  - **`index.html`**: `i18n.js?v=8`, `prompt-studio-beta.js?v=14`. **`sw.js`**: cache `elaris-v42`.
  - **Verified**: Studio Beta renders fully in FR and AR (labels, tabs, guide, dock, modal, trends panel, buttons, toasts); rapid EN↔FR↔AR switching + navigation throws no JS errors; RTL layout intact. (`net::ERR_FAILED` seen once during local testing was the SW cache-version swap mid-reload — transient, self-heals, not a code issue.)
- **2026-09-10**: **i18n Phase 2b — Studio Beta Piece Configurator + Archetype names translated** (`prompt-studio-beta.js` v16, `i18n.js` v9). User flagged these two as the most crucial scene-building controls and they were still English regardless of language.
  - **Piece Configurator**: the Category / Precious Metal / Center Gemstone `<option>` values now resolve through `this._t('ps_cat_'|'ps_mat_'|'ps_stone_' + id.replace(/-/g,'_'), label)` — **reusing the existing master-studio keys** (`ps_cat_*`, `ps_mat_*`, `ps_stone_*` already had EN/FR/AR). No new keys needed.
  - **Archetypes (74)**: added `_archName(arch)` / `_archTag(arch)` helpers (`psb_arch_<id>_name` / `_tag`, id hyphens → underscores, EN fallback), wired into the Top-10 carousel cards, the Smart Guide header (render + `_updateSmartGuidePartial` partial update), and both archetype-modal card renders (`_renderModal` + `_renderModalGrid`). Added a **new `Object.assign` block "Phase 3a"** in `i18n.js` with all 74 archetype `name`+`tag` pairs for FR and AR (~296 strings). Archetype `name`/`tagline` extracted reliably from `prompt-studio.js` by per-object brace-matching (positional regex drifts).
  - **Still English** (data fields on the master archetype objects, not the pickable identity): each archetype's long `desc` and `bestFor` line shown only inside the modal card. Deferred to a later pass.
  - **`index.html`**: `i18n.js?v=9`, `prompt-studio-beta.js?v=16`. **`sw.js`**: cache `elaris-v44`.
  - **Verified**: FR + AR — all 3 configurator dropdowns and every archetype name/tagline (carousel, Smart Guide, Full Library modal) render in the picked language; no JS errors; RTL modal layout intact.
- **2026-09-11**: **i18n Phase 3 — old Prompt Studio (`prompt-studio.js` v65) wired to FR + AR**.
  - Added `_t(key, en)` + `_archName/_archTag` helpers (the archetype helpers reuse the **same `psb_arch_<id>_name/_tag` keys** as Studio Beta, so all 74 archetype names + taglines are now translated in the old studio too — `_renderArchetypeGrid` dropped its old 8-archetype `ps_arch_<prefix>_title` mapping). `init()` now calls `I18n.applyLanguage()` after render.
  - Wrapped the UI-structure strings in `_render()`, `_buildSmartGuide()`, `_renderSetComposition()`, the generated-prompt cards and all 12 `Elaris.toast(...)` calls: card titles (Brand Identity, Model & Human Elements, Modifiers, Advanced Controls…), every form label + chip label (gender, skin tone, ethnicity, hijab style, brand-touch, realism level, image-ref), explainer `<p>` copy, the Modifiers section (Camera Angle / Lighting & Mood / Lens Preset), Scene Realism block, Smart Guide (header, empty states, "Best Angles/Lighting/Camera", "Pro Tips"), Set Includes selector, Generated Prompts / Copy All, Quick Tips, history empty state, prompt-card buttons (New / Refine / Copy / Similar).
  - **New `Object.assign` block "Phase 3"** in `i18n.js` (~95 keys each for FR + AR). Skipped keys that already existed in the original dict (`ps_modifiers`, `ps_copy`, `ps_best_for`, `ps_none`, …) — those resolve to the existing translations.
  - **Still English** (deferred data-value translations, same policy as Studio Beta): the modifier *option values* (angle/lighting/surface/palette/styling/lens chip labels — photography terminology from the data arrays), each archetype's `bestFor` category list, and the per-archetype Smart-Guide `tips` sentences.
  - **`index.html`**: `i18n.js?v=10`, `prompt-studio.js?v=65`. **`sw.js`**: cache `elaris-v45`.
  - **Verified**: old Prompt Studio renders in FR and AR — card titles, labels, chips, archetype names/taglines, Smart Guide, buttons, tips; live switch re-renders; RTL layout intact; no JS errors.
  - **Remaining**: P4 (Motion Studio / Generate / Enhance / Batch / Watermark / PWA), P5 (Arabic native review), and the deferred data-value translations across both studios if desired.
- **2026-09-11**: **i18n Phase 5 — Arabic consistency / quality pass** (`i18n.js` v11). Self-review of every Arabic string added in P1–P3 (P4 skipped at user request).
  - **Biggest fix — "prompt" term**: newer keys were mixing Latin `prompt` / `الـ prompt` into Arabic sentences; the app's established term (original dict: `nav_promptstudio` = "ستوديو الموجهات", `gen_prompt` = "موجه الصورة") is **"موجّه" / pl. "موجهات"**. Aligned ~25 values across Trends, Studio Beta and old Prompt Studio (titles, generate buttons, "compiled prompt", history empty states, all toasts, tips, the trend "armed" indicator). No Latin `prompt` remains in any rendered Arabic string (verified).
  - **Other AR fixes**: `ps_lens_auto_note` gender agreement (الزاوية is fem. → "تحدِّد الزاوية…"); `ps_bt_wordmark` "كلمة ELARIS" → "شعار ELARIS النصّي" (text logo, not "the word"); `ps_toast_caption_copied` → app-standard "التسمية التوضيحية"; "viral" → idiomatic "رائج/رائجة" (was "منتشرة") in the Social & Viral group + `trd_cat_viral-format`; stale badge "72يوم من العمر" → "72 يوماً مضت" (`trd_days_old` = " يوماً", `trd_old` = "مضت"); dropped a leading arrow from `trd_use_studio`.
  - **FR polish**: `psb_title` "Prompt Studio Bêta" → "Studio Prompt Bêta" (matches `nav_promptstudio` = "Studio Prompt"); `psb_subtitle` reworded ("…, esthétique Liquid Glass"). FR "prompt" left as-is — it's a standard loanword and the original FR dict uses it too.
  - **`index.html`**: `i18n.js?v=11`. **`sw.js`**: cache `elaris-v46`.
  - **Verified**: Studio Beta / old Prompt Studio / Trends all render clean Arabic with no script-mixing; stale-badge string reads naturally; FR intact; no JS errors.
  - **Note**: this was a self-review by the model, not a human native-speaker sign-off — a native pass is still worthwhile before any public launch, especially for luxury-brand tone.

- **2026-09-11**: **iOS 26 Liquid Glass rolled out app-wide** — new global layer `css/ios26.css` (~1,400 lines) promotes the Studio Beta design language to every page. Chosen approach: *exact Studio Beta palette* (gold accent + blue/orange/purple/teal mesh) with a *skin + shell rebuild*.
  - **Strategy — token re-pointing, not a rewrite.** `ios26.css` loads after `styles.css` and re-points the legacy tokens (`--bg-card`, `--border`, `--radius-*`, `--accent`, `--shadow-*`, `--transition`) at iOS 26 values, so every component inherits Liquid Glass with **no JS markup changes**. Critically it also re-points **`--moroccan-bronze` → `#fbbf24`** (`#b45309` in light): 51 rules across `styles.css` reference that token directly (archetype "Best for" lines, badges, rails, links), so one declaration converts them all. The rest of the Heritage palette (`--sterling`, `--warm-sand`, `--atlas-cedar`, `--olive-mist`, `--night-slate`, `--elaris-light`) is re-pointed alongside it.
  - **App-wide mesh**: the existing `.bg-ambience` wrapper is reused as the animated mesh surface (same hues, positions and 24s `alternate` float as `.psb-mesh`); its two blurred bronze `.glow` orbs are retired via `display:none`.
    - **New knob `--i26-mesh-*`**: Beta's 3 columns are tuned to a full-strength mesh, but app-wide the same lobes span far more width and the hot orange/teal started eating body copy. The four hues are painted at **78%** alpha. Set them back to the solid hex values for Beta-identical intensity — that is the single dial for the whole backdrop.
  - **Shell rebuilt** as true iOS 26 components: floating glass sidebar (28px radius, 14px rail, diagonal sheen, logical `margin-inline-end:0` so it mirrors in Arabic), pill nav items with gold active state, iOS **segmented** language switcher, iOS **switch** theme toggle, glass hamburger, glass toasts with spring entry, glass PWA install sheet.
  - **Per-module glass**: `.ps-*` (old Prompt Studio), `.ms-*` (Motion), `.wm-*` (Watermark, incl. iOS switches), Trends, Batch, Captions, Generate/Enhance/Templates/Gallery — one shared pill-chip language across all of them.
  - **`index.html`**: `ios26.css?v=9` added after `beta.css`; nav sections wrapped in `.i26-nav-scroll` (scroll band so the footer can't be crushed); `theme-color` → `#0e1020`. **`sw.js`**: cache `elaris-v47`, now precaches `beta.css` + `ios26.css` (`beta.css` had been missing).
  - **Studio Beta untouched** — verified `beta.css` references **zero** legacy tokens, so the re-point cannot reach it; it still paints its own opaque `.psb-mesh` and owns `.psb-*`. It only gains the matching glass shell.
  - **Bugs found and fixed during verification**: (1) a `background:` shorthand in the light-mode field rule erased the custom select chevron — rebuilt with longhands plus a matching-specificity light selector, and mirrored for RTL (the chevron is two gradients, so a single `background-position` flip splits it); (2) the desktop sidebar's `z-index:10` sits outside any media query and was overriding the mobile `z-index:100`, dropping the drawer *under* `.sidebar-overlay` so the overlay's blur smeared the drawer's own content; (3) the drawer's `.open`/`.active` classes are never cleared when the window grows back past the breakpoint — harmless at the old 2px blur, but it frosted the whole app at 10px, so the overlay is now hard-disabled above 769px.
  - **Verified**: Prompt Studio, Studio Beta, Motion Studio, Generate, Captions, Trends, Batch, Watermark — all render glass in **dark and light**; Arabic RTL mirrors the floating sidebar (rail gap resolves on the right) and the select chevron; mobile drawer slides and reads crisply; **no console errors** on any page.
  - **Note**: `styles.css` still holds ~27 hardcoded `rgba(166,124,82,…)` values. The visible ones are overridden in `ios26.css`; the remainder sit on surfaces that are themselves re-skinned. A later cleanup pass could delete them outright.
- **2026-09-11**: **Specular / lensing pass — moving the glass from "frosted" toward true Liquid Glass.** New `js/ios26.js` + three additions to `css/ios26.css`. The prior pass delivered blur + tint + a static sheen, which is the iOS 7–18 *materials* model; Liquid Glass's defining trait is that it behaves like a **lens**, so its highlight and its edge respond to the light source.
  - **Pointer-tracked specular** (ported from the `.psb-panel` mechanic at `prompt-studio-beta.js:1487`, which had never left Studio Beta — there were literally 0 `.psb-spot` instances anywhere else). `js/ios26.js` writes `--i26-mx` / `--i26-my` on whichever glass surface the pointer is over, rAF-throttled, via one delegated `pointermove` listener. Surfaces: `.card`, `.ps-panel`, `.trend-card`, `.wm-guide`, `.ms-prompt-block`, `.ps-prompt-block`, `.sidebar`.
  - **`::before` rebuilt** to carry two background layers in the surface's own coordinate space — the tracked highlight (strength = `--i26-spec`) plus the existing 115° sheen. It was previously an oversized `-60%/220%` box, which made percentage-based highlight positioning meaningless; now `inset: 0`. `--i26-spec` is registered with **`@property`** so the highlight can fade in/out; without `@property` support it still applies, just snaps.
  - **`::after` is a new specular rim** (edge lensing): a 1.5px ring drawn by masking a radial gradient to the padding box (`mask-composite: exclude`, `-webkit-` prefixed). It is brightest where the pointer is and falls off around the border, so the edge catches light **directionally** instead of being a uniform hairline. `::after` was verified free on every glass surface first.
  - **New `--i26-thickness` token**: three inset shadows giving the glass a *body* — lit top inner edge, shadowed bottom, soft left. Frosted panes are flat; Liquid Glass has depth. Appended to the base `box-shadow` (light mode gets its own, much softer values).
  - **Scroll edge effect**: `.main-content::before` is a sticky 74px `blur(12px)` band, mask-faded to transparent and pulled back by its own negative margin so it costs no layout space. `js/ios26.js` toggles `.i26-scrolled` past an 8px threshold, so it only appears once there is content beneath it to dissolve.
  - **Accessibility**: the tracking is gated on `prefers-reduced-motion` (WCAG 2.3.3 — motion from interaction must be disableable), checked **per-event** rather than at bind time so the setting can change mid-session. Degradation is deliberate and verified: rim, sheen and thickness are pure CSS and survive, with the highlight parked at `50% 0%` — a fixed overhead light rather than a tracking one. Touch pointers are ignored outright (a finger is not a light source).
  - **Verified**: tracking writes exactly `mx 70.0% / my 25.0%` for a pointer at 70%/25% of a card; leaving a surface clears its props and the next surface picks them up; touch events ignored; scroll band off at top, on at 600px, off again at 4px. Dark + light both clean, no console errors. *(Note for future sessions: the automation browser reports `prefers-reduced-motion: reduce` and throttles rAF while the pane is hidden — verifying this code needs `MediaQueryList.prototype.matches` faked and `requestAnimationFrame` shimmed synchronously, and the rAF shim must be installed **before** the first dispatch or the `ticking`/`frame` latch swallows it.)*
  - **`index.html`**: `ios26.css?v=10`, new `js/ios26.js?v=2`. **`sw.js`**: cache `elaris-v48`, precaches `js/ios26.js`.
  - **Still missing for full fidelity** (unchanged from the earlier assessment): true refraction (`feDisplacementMap` on `backdrop-filter` — Chrome-only, unusable in Safari, so deliberately skipped), adaptive contrast that samples the backdrop, concentric nested radii, and morphing control transitions.
- **2026-09-11**: **Shadow system rebuilt, lens toned down, and Prompt Studio restructured for progressive disclosure.**
  - **Shadow bleed (user-reported bug)**: with the mobile nav *closed*, a shadow covered ~15% of the screen from the left, sitting over the content. Cause: the mobile sidebar carried `box-shadow: 0 0 80px rgba(0,0,0,0.60)` **unconditionally**. A centred 80px blur on an element parked at `translateX(-100%)` spills its full radius back across the viewport — measured at exactly 80px over a 375px screen = **21%**. Fix: no shadow while closed; `.sidebar.open` gets a directional `10px 0 34px -8px` (mirrored for RTL) so the blur falls away from the screen edge.
  - **Shadow system** was the same mistake at smaller scale — single heavy blurs at zero offset (`0 14px 50px /.42`, hovers to `0 18px 60px /.50`) that haloed sideways and muddied the mesh. Replaced with a **two-layer scale**: tight contact shadow + ambient one with *negative spread* so it stays under the element. Tokens `--i26-shadow` / `--i26-shadow-sm` / `--i26-shadow-lift`; every ad-hoc hover shadow now references the lift token. **No shadow in the file exceeds a 30px blur.** The inset highlight inside `--i26-shadow` was doubling with `--i26-thickness` and was removed.
  - **Lens toned down** (user: "looks cheap"): rim peak alpha **0.90 → 0.30**, radius 340px → 170px with a faster falloff so only a short arc of border lights rather than the entire outline; tracked glow **0.20 → 0.07**. The rim is now scoped **off** the small repeating surfaces (trend cards, prompt blocks, guides) where a ring on every tile read as decoration — it remains on `.card` and `.ps-panel` only. Both strengths are single tokens, **`--i26-rim-alpha`** and **`--i26-spec-alpha`**; setting either to `0` switches that half off entirely.
  - **Prompt Studio progressive disclosure** — the headline change. The configurator was five stacked cards and ~185 chips, all expanded: **8.5 screens (7,654px)** before reaching Generate.
    - `_applyTabs()` / `_setTab()` added to `prompt-studio.js`. The four secondary cards are tagged `data-ps-tab="brand|model|modifiers|advanced"` and **moved in the DOM** into one `.ps-tabshell` with a segmented bar (same pattern as `.psb-mod-tabs`). "Describe Your Piece" stays open above as the primary input.
    - **Deliberately a DOM move, not markup surgery**: every `id` inside the cards is preserved, and since the app's cycle is `_render() → _renderArchetypeGrid() → _bind()`, all existing wiring re-queries and keeps working untouched. `_applyTabs()` is called at the end of `_render()`.
    - **The open tab lives on the module (`this._activeTab`), not in the DOM** — `_render()` fires on nearly every chip click and rebuilds `innerHTML`, so a DOM-held tab would reset on every interaction. Verified: clicking a chip inside Modifiers leaves Modifiers open.
    - **Archetype grid capped** at the top 8 (was rendering all 74 = ~37 rows, the second-biggest height driver), with a `.ps-arch-more` expander. **Anything already selected is always kept visible**, so collapsing can never hide an active choice — verified by selecting rank 41 ("Surreal Animal") and collapsing: 8 + 1 shown.
    - Generate moved into a sticky `.ps-generate-sticky` bar with its own mask-faded blur, so it no longer scrolls away below the grid.
    - **Result**: Brand **1.4** / Model **1.4** / Advanced **2.4** / Modifiers **4.1** screens, vs 8.5 before. Modifiers is the outlier — it holds Camera Angle + Lighting & Mood + Lens Preset chip groups; sub-grouping it further is the obvious next step if wanted.
  - **i18n Phase 4**: new `Object.assign` block in `i18n.js` for the tab labels and archetype expander (FR + AR) — `ps_tab_brand/model/modifiers/advanced`, `ps_arch_show_all/_archetypes/_show_less`. Verified in all three languages; AR reads "عرض كل 74 نموذجاً".
  - **`index.html`**: `ios26.css?v=12`, `prompt-studio.js?v=66`, `i18n.js?v=12`. **`sw.js`**: cache `elaris-v50`.
  - **Verified**: tab switch → correct panel; tab survives a full `_render()`; archetype select ("1 selected"); expand 8 → 74 → 8; end-to-end Generate produced 4 prompt blocks (first 2,224 chars) and 2 history entries; mobile icons-only tab bar with no shadow bleed; RTL tab clicks map to the right panel; no console errors anywhere.
